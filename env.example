# Copy this file to .env and fill in your RateHawk credentials when you have them.
# Leave this file as-is (or leave .env missing) to keep running in Demo Mode.

RATEHAWK_API_KEY_ID=
RATEHAWK_API_KEY=

# Port the server runs on (optional, defaults to 3000)
PORT=3000
